FROM nginx:latest

# Install certbot and its nginx plugin so this container can manage its own SSL.
RUN apt-get update && \
    apt-get install -y certbot python3-certbot-nginx && \
    rm -rf /var/lib/apt/lists/*


